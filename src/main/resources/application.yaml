spring:
  main:
    banner-mode: off
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

voice-to-db:
  upload-root-directory: upload
  result-root-directory: results
  vosk:
    script-path: 'python/vosk_voice.py'
---
spring:
  config:
    activate:
      on-profile: docker

voice-to-db:
  upload-root-directory: /upload
  result-root-directory: /results
  vosk:
    script-path: '/app'
---

